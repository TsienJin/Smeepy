{
  "name": "paddock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:build\" \"npm run dev:start\"",
    "dev:build": "swc src -w --out-dir dist",
    "dev:start": "nodemon --watch \"dist/**/*\" -e js ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "swc src -d dist",
    "start": "NODE_ENV=production node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.87",
    "@types/node": "^20.6.3",
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1",
    "typescript": "^5.2.2"
  }
}
